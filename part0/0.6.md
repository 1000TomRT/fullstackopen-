sequenceDiagram
    participant browser
    participant server

    Note right of browser: El usuario escribe la nota y hace clic en "Save"
    Note right of browser: El código JS intercepta el envío, añade la nota a la lista local y redibuja el DOM inmediatamente

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of browser: Payload JSON: { "content": "mi nueva nota SPA", "date": "2026-08-09" }
    activate server
    Note over server: El servidor guarda la nota enviada en el JSON
    server-->>browser: HTTP Status 201 Created {"message": "note created"}
    deactivate server
